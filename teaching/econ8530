<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>ECO8000 — Course Materials</title>
  <style>
    :root{
      --bg:#ffffff;
      --text:#111827;
      --muted:#6b7280;
      --border:#e5e7eb;
      --link:#1d4ed8;
      --link-hover:#1e40af;
      --card:#f9fafb;
    }
    body{
      margin:0;
      font-family: system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, "Apple Color Emoji","Segoe UI Emoji";
      background:var(--bg);
      color:var(--text);
      line-height:1.55;
    }
    .wrap{
      max-width: 900px;
      margin: 0 auto;
      padding: 28px 18px 60px;
    }
    header{
      padding: 10px 0 18px;
      border-bottom: 1px solid var(--border);
      margin-bottom: 18px;
    }
    h1{
      font-size: 1.9rem;
      margin: 0 0 6px 0;
      letter-spacing: -0.02em;
    }
    .subtitle{
      margin:0;
      color:var(--muted);
    }
    nav{
      margin-top: 14px;
      display:flex;
      flex-wrap:wrap;
      gap:10px 14px;
      font-size: 0.95rem;
    }
    nav a{
      color:var(--link);
      text-decoration:none;
    }
    nav a:hover{ color:var(--link-hover); text-decoration:underline; }

    section{
      margin-top: 22px;
      padding: 16px 16px 6px;
      border: 1px solid var(--border);
      border-radius: 12px;
      background: var(--card);
    }
    h2{
      margin: 0 0 10px 0;
      font-size: 1.2rem;
    }
    ul{ margin: 0 0 12px 18px; padding:0; }
    li{ margin: 8px 0; }
    a{ color:var(--link); }
    a:hover{ color:var(--link-hover); }

    .meta{
      display:flex;
      flex-wrap:wrap;
      gap:10px 18px;
      margin-top: 10px;
      color: var(--muted);
      font-size: 0.95rem;
    }
    .tag{
      display:inline-block;
      padding: 2px 8px;
      border: 1px solid var(--border);
      border-radius: 999px;
      background:#fff;
      color: var(--muted);
      font-size: 0.85rem;
      vertical-align: middle;
      margin-left: 6px;
    }
    footer{
      margin-top: 22px;
      color: var(--muted);
      font-size: 0.9rem;
    }
    .two-col{
      display:grid;
      grid-template-columns: 1fr;
      gap: 14px;
    }
    @media (min-width: 820px){
      .two-col{ grid-template-columns: 1fr 1fr; }
    }
  </style>
</head>

<body>
  <div class="wrap">
    <header>
      <h1>ECO8000 — Course Materials</h1>
      <p class="subtitle">Université du Québec à Montréal • Hiver 2026</p>

      <div class="meta">
        <div><strong>Instructor:</strong> Giacomo Rella</div>
        <div><strong>Email:</strong> <a href="mailto:rella.giacomo@uqam.ca">rella.giacomo@uqam.ca</a></div>
        <div><strong>Last updated:</strong> <span id="last-updated">2025-12-22</span></div>
      </div>

      <nav>
        <a href="#syllabus">Syllabus</a>
        <a href="#problem-sets">Problem Sets</a>
        <a href="#lectures">Lecture Notes</a>
        <a href="#lectures">Lecture Slides</a>
        <a href="#readings
